using UnityEditor;
using UnityEditor.Animations;
using UnityEngine;

namespace Test
{
    public class Test : MonoBehaviour     
    {
        public void Start()
        {
            //AnimatorController ani = new AnimatorController();
            //AnimatorStateMachine stateMachine = ani.layers[0].stateMachine;

            ////读取动画文件
            //var data = AssetDatabase.LoadAllAssetsAtPath("ssss");
            //foreach (Object o in data)
            //{
            //    if (o is AnimationClip)
            //    {
                    
            //    }
            //}

            //AnimatorStateTransition transition = new AnimatorStateTransition();
            ////transition.AddCondition(, );
            ////AnimatorConditionMode.If----true
            ////AnimatorConditionMode.IfNot----false
            
            
        }
    }
}
